# TODO: Option to disable AVX
# TODO: Option to enable AVX2

flags = ["-O2", "-m64", "-mavx", "-msse4.2", "-flto", "-idirafter ../../include/sys"]
debug_flags = ["-Og", "-g", "-mavx",  "-msse4.2", "-m64",  "-Wpedantic", "-idirafter ../../include/sys"]
quick_flags = ["-O", "-m64", "-msse4.2", "-idirafter ../../include/sys"]

Import('ARCH')
Import('BUILD_TYPE')
Import('TUNE')
Import('DEFINES')

env = Environment( CPPPATH = "../../include" )

flags.append("-march=" + ARCH)
debug_flags.append("-march=" + ARCH)
quick_flags.append("-march=" + ARCH)

flags.append("-mtune=" + TUNE)
debug_flags.append("-mtune=" + TUNE)
quick_flags.append("-mtune=" + TUNE)

if BUILD_TYPE == "FINAL":
    env.Append( CPPFLAGS = flags, CPPDEFINES = DEFINES, LIBS = ['bullet3', 'box2d'] ) # TODO: Proper library names
    env.Append(CPPDEFINES = { "_PRODUCTION_" : None })
elif BUILD_TYPE == "DEBUG":
    env.Append( CPPFLAGS = debug_flags, CPPDEFINES = DEFINES, LIBS = ['bullet3', 'box2d'] )
    env.Append(CPPDEFINES = { "_DEBUG_" : None })
elif BUILD_TYPE == "QUICK":
    env.Append( CPPFLAGS = quick_flags, CPPDEFINES = DEFINES, LIBS = ['bullet3', 'box2d'] )
    env.Append(CPPDEFINES = { "_QUICK_" : None })

Object('physmain.cpp')
Object('step.cpp')
Object('rb2d.cpp')